# Ziptron Environment Variables

# Backend API Base URL
EXPO_PUBLIC_API_BASE_URL=https://your-api-url.com/api/v1

# Google Maps API Key for directions and map display
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Clerk Authentication (Publishable Key is already in code)
# Note: For production, move this to environment variables as well
